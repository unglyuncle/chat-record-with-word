# MeetingTranscriber 使用手册

本文档面向实际使用者，说明如何录音转写、如何选择识别方式、如何下载离线模型，以及设置页里各个参数怎么调。

## 1. 快速开始

1. 打开 App，进入主界面。
2. 首次使用建议先进入右上角“设置”。
3. 在“转写方式”中选择：
   - 推荐离线使用：`离线识别（不联网）`
   - 需要云端高准确率时：选择讯飞、阿里或火山在线识别，并填写对应密钥
4. 如果使用离线识别，先在“离线语音包管理”中下载所需模型。
5. 回到主界面，点击底部麦克风按钮开始录音。
6. 录音过程中会实时显示转写文字。
7. 点击停止后，App 会保存录音和转写记录；如果开启了说话人校正，会在停止后继续整理一小段时间。

## 2. 主界面使用

### 开始/停止录音

- 点击底部麦克风按钮开始录音。
- 录音中点击红色停止按钮结束。
- 录音中可以暂停/继续。

### 查看实时转写

- 转写内容会像聊天记录一样从下往上累积。
- 正在识别但还没最终确认的文字会显示为临时片段。
- 如果手动上滑查看旧内容，自动滚动会暂停；点“回到底部”后恢复自动跟随。

### 播放对应录音

- 点击某一条转写片段，可以播放该句附近的录音。
- 录音结束后，也可以在历史记录中查看和播放。

### 导出

主界面顶部提供：

- `MD`：导出 Markdown 文本
- `HTML`：导出 HTML 文档

没有转写内容时不能导出。

## 3. 转写方式怎么选

### 离线识别（推荐默认）

特点：

- 不联网。
- 首次需要下载模型。
- 隐私最好，录音和识别都在本机处理。
- 对手机性能和内存有要求。

适合：

- 会议记录
- 课堂录音
- 不方便联网或不想上传音频的场景

### 讯飞在线识别

特点：

- 需要联网和讯飞密钥。
- 可开启说话人区分，但具体参数与开通产品有关。
- 适合追求云端稳定性和实时效果的场景。

需要填写：

- 应用编号
- 接口密钥
- 是否自动区分说话人

### 阿里在线识别

特点：

- 需要联网和 DashScope API Key。
- 实时接口暂不做说话人分离。
- 默认模型为 `paraformer-realtime-v2`。

需要填写：

- 阿里密钥
- 识别版本

### 火山在线识别

特点：

- 需要联网和火山引擎相关参数。
- 适合已开通火山豆包语音识别服务的用户。

需要填写：

- 应用编号
- 访问密钥
- 资源编号，默认 `volc.bigasr.sauc.duration`

## 4. 离线语音包怎么选

### SenseVoice Small int8

推荐级别：首选默认。

特点：

- 体积适中。
- 支持中文、英文、日文、韩文、粤语等。
- 对手机压力相对较小。
- 适合大多数日常会议。

建议：

- 不确定选什么，就选这个。

### Paraformer zh int8

推荐级别：中文会议常用。

特点：

- 更偏中文普通话。
- 适合中文会议、课堂、访谈。
- 标点模型是可选后处理；没有标点模型也能识别，只是标点可能少一些。

建议：

- 中文普通话为主，可以试这个。

### Zipformer CTC zh int8

推荐级别：需要更快响应时使用。

特点：

- 流式模型，边说边出字更快。
- 准确率可能不如较大的离线模型。
- 适合实时预览优先的场景。

建议：

- 想要“马上出字”，选这个。

### Zipformer CTC zh XLarge int8

推荐级别：高性能手机再试。

特点：

- 更大，内存占用更高。
- 低内存手机可能加载慢或失败。

建议：

- 普通手机不建议默认使用。
- 如果 App 变慢或崩溃，换回普通 Zipformer 或 SenseVoice。

### FunASR Nano int8

推荐级别：实验/高内存设备。

特点：

- 模型较大。
- 对手机内存和性能要求高。

建议：

- 只有在手机内存充足时使用。
- 如果出现启动慢、发热、闪退，换回 SenseVoice 或 Paraformer。

### Qwen3-ASR 0.6B int8

推荐级别：实验/最高成本。

特点：

- 模型很大。
- 加载和推理成本最高。
- 不适合低内存手机。

建议：

- 仅建议在高端设备上测试。
- 日常会议不建议首选。

## 5. 离线语音包管理

设置页中的“离线语音包管理”用于下载、删除、重新下载模型。

常见组件：

- `SenseVoice Small int8`：SenseVoice 主模型
- `Silero VAD`：语音活动检测，用于判断什么时候有人说话
- `Paraformer zh int8`：Paraformer 主模型
- `Zipformer CTC zh int8`：普通流式模型
- `Zipformer CTC zh XLarge int8`：大号流式模型
- `FunASR Nano int8`：大模型识别包
- `Qwen3-ASR 0.6B int8`：大模型识别包
- `CT-Transformer punctuation`：标点模型，可选
- `Pyannote speaker segmentation`：说话人分段模型
- `3D-Speaker CAM++ embedding`：声纹模型

### 下载建议

- 使用稳定 Wi-Fi。
- 下载过程中不要频繁切后台或清理 App。
- 大模型下载时间较长，耐心等待进度结束。

### 删除/重下建议

如果出现下面情况，优先删除对应模型并重新下载：

- 某个模型突然不能用。
- logcat 出现 `Load model from ... failed`。
- logcat 出现 `Protobuf parsing failed`。
- 下载完成后立刻闪退。
- 换手机后模型表现异常。

这类问题通常是模型文件下载不完整或解压损坏。

## 6. 使用场景预设

如果不想手动理解每个参数，优先使用“使用场景预设”。预设会自动设置识别灵敏度、小声增强、收音方式、AGC 和说话人整理策略。

### 安静会议室

默认推荐。

适合：

- 普通会议室
- 手机放在桌面中间
- 背景噪声不明显

自动设置特点：

- 灵敏度适中
- 小声增强适中
- 使用兼容性最好的自然收音
- 录完后自动整理说话人

### 远距离/大会议室

适合：

- 手机离说话人较远
- 大会议桌
- 课堂、讲座
- 经常漏识别小声说话

自动设置特点：

- 降低 VAD 门槛，更容易捕捉人声
- 提高小声增强
- 开启系统自动增益
- 使用偏语音识别的收音方式

注意：

- 噪声也可能被放大。
- 如果出现乱字或噪声片段，换回“安静会议室”或“嘈杂环境”。

### 嘈杂环境

适合：

- 背景人声多
- 空调、风扇、路噪明显
- 经常把噪声识别成文字

自动设置特点：

- 提高 VAD 门槛，减少误触发
- 降低小声增强
- 使用更强的系统降噪收音
- 说话人阈值略高，减少同一个人被拆成多人

注意：

- 说话人离手机太远时可能漏识别。

### 近距离采访

适合：

- 手机靠近说话人
- 一对一访谈
- 桌面近距离录音

自动设置特点：

- 降低增益，避免声音过度放大
- 保持自然音质
- 开启录音时说话人临时标注
- 录完后继续做说话人校正

### 长会议省电

适合：

- 一小时以上会议
- 手机容易发热
- 只需要稳定保存和基础转写

自动设置特点：

- 降低增益和误触发概率
- 关闭实时说话人标注
- 关闭录后整段说话人整理
- 减少停止录音后的等待时间

### 自定义

适合：

- 你想自己调高级参数
- 某个预设不完全适合当前环境

只要手动修改下面这些参数，App 会自动切到“自定义”：

- 录音时先标出说话人
- 录完后自动整理说话人
- 区分人数阈值
- 更容易听到人声
- 小声增强
- 收音方式
- 自动放大小声说话

## 7. 说话人区分设置

### 录音时先标出说话人

对应参数：`liveDiarization`

默认：关闭。

作用：

- 录音过程中就尝试给每段文字标出“说话人 1 / 2 / 3”。
- 速度快，但只是临时结果，可能不准。

建议：

- 手机性能较好：可以开启。
- 想减少耗电和发热：关闭。
- 会议很长：建议关闭，停止后再统一校正。

### 录完后自动整理说话人

对应参数：`postDiarization`

默认：开启。

作用：

- 停止录音后，对整段录音重新做说话人分离。
- 比实时标注更准。
- 会在录音结束后继续处理一段时间。

建议：

- 需要会议纪要：开启。
- 只要快速转文字：可以关闭。
- 超长会议、低内存手机：如果停止后处理很慢，可以关闭。

### 区分人数阈值

对应参数：`diarizationThreshold`

默认：`0.5`

范围：`0.3` 到 `0.8`

含义：

- 数值越低，越容易把声音分成不同说话人。
- 数值越高，越倾向于把相似声音合并成同一个人。

调参建议：

| 现象 | 调整方式 |
|---|---|
| 明明是同一个人，却被分成多人 | 调高，比如 `0.55` 到 `0.65` |
| 明明是不同人，却被合并成一个人 | 调低，比如 `0.4` 到 `0.45` |
| 会议人数少，声音差异明显 | `0.5` 通常可以 |
| 会议人数多，声音相近 | 先试 `0.45` |

## 8. 识别灵敏度设置

### 更容易听到人声

对应参数：`vadThreshold`

默认：`0.22`

范围：`0.08` 到 `0.6`

含义：

- 这是 VAD 判断“是否有人在说话”的门槛。
- 数值越低，越容易把声音当成人声。
- 数值越高，越不容易误触发。

调参建议：

| 场景/现象 | 推荐调整 |
|---|---|
| 说话人离手机远，漏识别 | 调低到 `0.15` 到 `0.2` |
| 说话声音小 | 调低 |
| 环境很吵，噪声也被识别 | 调高到 `0.28` 到 `0.4` |
| 经常出现空白片段或乱字 | 调高 |
| 普通会议室 | 保持 `0.22` |

### 小声增强

对应参数：`micGain`

默认：`4.0`

范围：`1.0` 到 `12.0`

含义：

- 在送入识别前放大录到的人声。
- 数值越高，小声越容易被识别。
- 但噪声也会被一起放大。

调参建议：

| 场景/现象 | 推荐调整 |
|---|---|
| 手机离人较远 | `4.0` 到 `8.0` |
| 声音很小，经常漏字 | 增大 |
| 环境噪声大，识别乱 | 降低到 `1.0` 到 `3.0` |
| 录音已经很响 | `1.0` 到 `2.0` |
| 普通会议 | 保持 `4.0` |

### VAD 与增益怎么搭配

推荐从默认值开始：

- `vadThreshold = 0.22`
- `micGain = 4.0`

如果漏识别：

1. 先把 `micGain` 提高到 `6.0`。
2. 还漏，再把 `vadThreshold` 降到 `0.18`。

如果噪声太多：

1. 先把 `micGain` 降到 `2.0`。
2. 还乱，再把 `vadThreshold` 提高到 `0.3`。

## 9. 录音保存与音质

### 保存方式

#### M4A/AAC

默认推荐。

优点：

- 文件小。
- 适合长会议。
- 分享方便。

缺点：

- 有损压缩。

适合：

- 日常会议
- 课堂录音
- 长时间录音

#### WAV

优点：

- 尽量保留原始声音。
- 后期处理更方便。

缺点：

- 文件很大。

适合：

- 需要保留高质量原始录音。
- 短时间测试。

### 收音方式

#### 自然收音

默认推荐。

特点：

- 优先使用普通麦克风输入。
- 兼容性最好。
- 适合大多数设备。

#### 突出人声

特点：

- 使用更偏语音识别的音频源。
- 可能会对人声更友好。
- 部分手机会做系统级处理。

适合：

- 普通收音漏字时尝试。

#### 强力降噪

特点：

- 使用通话处理相关音频源。
- 可能明显降噪，也可能损失音质。

适合：

- 环境很吵时尝试。
- 不建议默认使用。

### 自动放大小声说话

对应参数：`recordingAgc`

默认：关闭。

作用：

- 使用系统 AGC 自动增益控制。
- 能让小声更明显。
- 也可能让噪声变大，或让音量忽大忽小。

建议：

- 远距离会议可以尝试开启。
- 如果出现噪声变大、忽大忽小，关闭。

## 10. 推荐配置

### 普通中文会议

- 转写方式：离线识别
- 模型：SenseVoice Small int8 或 Paraformer zh int8
- 录音保存：M4A/AAC
- 收音方式：自然收音
- `vadThreshold = 0.22`
- `micGain = 4.0`
- 录完后自动整理说话人：开启

### 需要更快出字

- 转写方式：离线识别
- 模型：Zipformer CTC zh int8
- 标点模型：可不下载
- `vadThreshold = 0.22`
- `micGain = 4.0`

### 手机离说话人远

- `micGain = 6.0` 到 `8.0`
- `vadThreshold = 0.15` 到 `0.2`
- 收音方式先保持自然收音
- 如果仍然漏字，再试“突出人声”

### 环境噪声大

- `micGain = 1.0` 到 `3.0`
- `vadThreshold = 0.3` 到 `0.4`
- 可以尝试“强力降噪”
- 不建议开启过高增益

### 手机性能一般

- 模型：SenseVoice Small int8
- 不建议使用 Qwen3-ASR、FunASR Nano、Zipformer XLarge
- 实时说话人标注：关闭
- 录完后说话人整理：视会议长度决定

### 高性能手机

- 可以尝试 Paraformer、FunASR Nano、Qwen3-ASR
- 如果发热、卡顿、闪退，换回 SenseVoice 或普通 Zipformer

## 11. 常见问题

### 1. 所有模型都闪退怎么办？

优先看 logcat。如果看到：

```text
Load model from ... failed
Protobuf parsing failed
```

说明某个本地模型文件坏了。处理方法：

1. 在“离线语音包管理”中删除对应模型。
2. 重新下载。
3. 如果不知道是哪一个，先删除最近下载的模型和 `CT-Transformer punctuation` 标点模型。

### 2. 标点模型坏了会影响主识别吗？

新版逻辑中，标点是可选后处理。标点模型坏了或没下载时，主识别仍然可以运行，只是文本标点可能少一些。

### 3. 为什么一台手机可以，另一台手机不行？

模型是每台手机单独下载到自己的 App 私有目录。某台手机下载不完整，就只会影响那台手机。

常见原因：

- 下载中断
- 网络抖动
- 解压过程被系统清理或杀后台
- 存储空间不足
- 旧版本只检查文件是否存在，没有检查完整性

### 4. 识别不到人声怎么办？

按顺序尝试：

1. 确认麦克风权限已允许。
2. 把手机放近一些。
3. 提高 `micGain`。
4. 降低 `vadThreshold`。
5. 尝试“突出人声”收音方式。

### 5. 识别很多噪声怎么办？

按顺序尝试：

1. 降低 `micGain`。
2. 提高 `vadThreshold`。
3. 换到更安静的位置。
4. 尝试“强力降噪”。

### 6. 说话人数量不对怎么办？

- 同一个人被分成多个：调高 `diarizationThreshold`。
- 多个人被合并成一个：调低 `diarizationThreshold`。
- 超长会议处理慢：关闭实时说话人标注，或关闭录后自动整理。

### 7. 录音结束后还在处理，是卡住了吗？

不一定。开启“录完后自动整理说话人”时，停止录音后 App 会继续做整段说话人校正。会议越长，处理越久。

如果只想快速结束，可以关闭“录完后自动整理说话人”。

### 8. 大模型一定更好吗？

不一定。大模型可能更准，但也更慢、更耗电、更容易受手机内存影响。日常会议建议优先使用：

- SenseVoice Small int8
- Paraformer zh int8
- Zipformer CTC zh int8

## 12. 维护建议

- 模型下载后如果异常，先删除重下。
- 大模型不要一次性全下，按需要下载。
- 长会议建议使用 M4A/AAC 保存。
- 重要会议前先录 10 秒测试，确认能正常出字。
- 如果刚更新 App，建议重新打开设置页确认当前模型和参数。
- 遇到闪退时，优先查看 logcat 中 `Load model from` 后面的路径。
